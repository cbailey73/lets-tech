<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{post.title}}</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>

<body>

  <div class="container mt-4">
    <div class="card">
      <div class="card-body">
        <h2 class="card-title">{{post.title}}</h2>
        <p class="card-text">{{post.content}}</p>
        <p class="card-text">Posted by: {{post.user.username}} | {{format_time post.createdOn}}</p>
      </div>
    </div>

    <div class="mt-4">
        <h4>Comments</h4>
        <ul class="list-group">
            {{#each post.Comments}}
            <li class="list-group-item">
                <p>{{this.content}}</p>
                <small>Comment by: {{this.user.username}} | {{format_time this.createdOn}}</small>
            </li>
            {{/each}}
        </ul>

        <h5>Add a Comment</h5>
        <form action="api/post/{{post.id}}/addComment" method="POST">
        <div class="form-group">
            <textarea class="form-control" name="commentContent" rows="3" required></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Submit Comment</button>
        </form>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="../public/js/post.js"></script>
</body>
</html>
